You are running a self-contained file-work task inside the sample workspace.

Goal:
Create a small research bundle under `research/` that demonstrates careful multi-step work and uses the available tools repeatedly.

Requirements:
1. Start by listing files in the current directory so you understand the workspace.
2. Read at least three existing text-based files from the repository that help you understand TokenGuard or this sample.
3. Create `research/notes-1.txt` with a concise summary of what TokenGuard does.
4. Create `research/notes-2.txt` with a concise summary of how this console sample works.
5. Create `research/plan.txt` with a numbered plan for improving the sample to better demonstrate compaction.
6. Re-read the files you created and refine at least two of them with edits so the work is iterative, not one-shot.
7. Finish by creating `research/final-report.txt` containing:
   - the files you inspected,
   - the files you created,
   - one paragraph explaining why this task burns context in an agent loop.

Rules:
- Use tools instead of pretending files exist.
- Actually inspect file contents before summarizing them.
- Keep working until every required file exists and contains meaningful content.
- When all work is complete, respond with a brief completion report and end with the exact line `TASK_COMPLETE`.
